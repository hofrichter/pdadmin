'use strict';

describe('domains module', function() {

  beforeEach(module('domains'));

  describe('domains controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ctrl2test = $controller('DomainsCtrl');
      expect(ctrl2test).toBeDefined();
    }));

  });
});

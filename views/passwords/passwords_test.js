'use strict';

describe('accounts module', function() {

  beforeEach(module('accounts'));

  describe('accounts controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ctrl2test = $controller('AccountsCtrl');
      expect(ctrl2test).toBeDefined();
    }));

  });
});
